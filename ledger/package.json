{
  "name": "foresight-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "db:create": "wrangler d1 create foresight-ledger",
    "db:migrate:local": "wrangler d1 execute foresight-ledger --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute foresight-ledger --remote --file=schema.sql"
  },
  "engines": {
    "node": ">=18"
  }
}
